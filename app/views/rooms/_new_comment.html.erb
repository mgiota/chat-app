  <%= bootstrap_form_for([@room, @room.comments.build], remote: true) do |f| %>
    <p>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit "Submit", class: 'btn btn-success' %>
    </p>
  <% end %>